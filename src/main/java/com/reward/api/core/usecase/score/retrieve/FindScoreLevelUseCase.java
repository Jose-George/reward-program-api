package com.reward.api.core.usecase.score.retrieve;

import com.reward.api.core.usecase.UseCase;
import com.reward.api.core.usecase.score.common.ScoreDataOutput;

public abstract class FindScoreLevelUseCase extends UseCase<FindScoreLevelCommand, ScoreDataOutput> {
}
